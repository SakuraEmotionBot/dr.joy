{
  "name": "typescript",
  "version": "1.0.0",
  "description": "",
  "main": "greeter.js",
  "scripts": {
    "dev": "watch 'npm run build' src",
    "build": "babel src -d build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "build": "^0.1.4",
    "watch": "^1.0.2"
  }
}
